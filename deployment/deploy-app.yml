---

- name: Update the application to the latest version
  hosts: "{{ target|default('staging') }}"
  remote_user: brouwdag
  environment:
    PATH: "/opt/bin:/usr/pgsql-9.1/bin:{{ ansible_env.PATH }}"
  roles:
    - env
    - npm
    # - gulp
    - jspm
    - django
    - supervisor
    - nginx
