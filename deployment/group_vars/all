repository: https://github.com/modelbrouwers/modelbouwdag.nl.git

target_env: "{{ target|default('staging') }}"
release: "{{ release_tag|default('master') }}"

application_user: brouwdag
project_name: "modelbouwdag"

prefix: "{{ project_name + '_' + target_env }}"
project_home: "{{ ansible_env.HOME }}/django/{{ target_env }}/"
venv: "{{ ansible_env.HOME }}/.virtualenvs/{{ target_env }}"

settings_module: "modelbouwdag.conf.{{ target_env }}"
