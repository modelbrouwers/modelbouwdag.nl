{% for name, value in env_vars.items() -%}
{% if value is mapping -%}
{% for key, val in value.items() -%}
export {{ name|upper + '_' + key|upper }}="{{ val }}"
{% endfor -%}
{% else -%}
export {{ name|upper }}="{{ value }}"
{% endif -%}
{% endfor -%}
