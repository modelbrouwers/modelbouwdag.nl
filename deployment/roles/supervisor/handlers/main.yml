---

- name: restart uwsgi
  supervisorctl: name=uwsgi-{{ prefix }} state=restarted
  remote_user: root

- name: reread uwsgi confg
  supervisorctl: name=uwsgi-{{ prefix }} state=reread
  remote_user: root
